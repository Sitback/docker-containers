sudo: required

language: node_js

node_js:
  - "0.12"

services:
  - docker

before_install:
  - bash _scripts/login.sh
  - node _scripts/build.js

script:
  - docker run -d -p 80:80 chinthakagodawita/soe:php5.5
