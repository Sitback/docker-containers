app:
  build:
    image: web-containers-build
    dockerfile_path: Dockerfile.codeship
    args:
      storage-driver: overlay
  add_docker: true
  cached: true
  volumes_from:
    - dockerdata

dockerdata:
  image: busybox
  volumes:
    - /var/lib/docker
    - ./:/code
