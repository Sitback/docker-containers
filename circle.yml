machine:
  ruby:
    version: 2.1.0
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"

  override:
    - ruby _scripts/ci-build.rb

test:
  override:
  - echo "Container tests go here."

deployment:
  hub:
    branch: master
    commands:
      - ruby _scripts/publish.rb
